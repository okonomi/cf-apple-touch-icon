{
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"deploy": "npx wrangler publish --name edge-apple-touch-icon",
		"dev": "npx wrangler dev --local"
	},
	"devDependencies": {
		"wrangler": "^2.0.0"
	}
}
